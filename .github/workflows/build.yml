name: Build

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-ffmpeg-linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            pkg-config \
            yasm \
            nasm \
            curl \
            tar \
            xz-utils \
            autoconf \
            automake \
            libtool

      - name: Download LAME
        run: |
          curl -L \
            https://downloads.sourceforge.net/project/lame/lame/3.100/lame-3.100.tar.gz \
            -o lame.tar.gz

          tar xf lame.tar.gz

      - name: Build LAME
        run: |
          cd lame-3.100

          ./configure \
            --disable-shared \
            --enable-static \
            --prefix="$PWD/../lame-build"

          make -j"$(nproc)"
          make install

      - name: Download FFmpeg
        run: |
          curl -L \
            https://ffmpeg.org/releases/ffmpeg-8.1.1.tar.xz \
            -o ffmpeg.tar.xz

          tar xf ffmpeg.tar.xz

      - name: Configure FFmpeg
        run: |
          cd ffmpeg-8.1.1

          export PKG_CONFIG_PATH="$PWD/../lame-build/lib/pkgconfig"

          ./configure \
            --prefix="$PWD/build" \
            --pkg-config-flags="--static" \
            --extra-cflags="-I$PWD/../lame-build/include" \
            --extra-ldflags="-L$PWD/../lame-build/lib" \
            \
            --disable-debug \
            --disable-doc \
            --disable-ffplay \
            --disable-ffprobe \
            --disable-network \
            --disable-everything \
            \
            --enable-ffmpeg \
            \
            --enable-protocol=file \
            --enable-protocol=pipe \
            \
            --enable-libmp3lame \
            \
            --enable-demuxer=mp3 \
            --enable-demuxer=wav \
            --enable-demuxer=ogg \
            --enable-demuxer=flac \
            --enable-demuxer=mov \
            --enable-demuxer=matroska \
            --enable-demuxer=aac \
            --enable-demuxer=ape \
            --enable-demuxer=asf \
            --enable-demuxer=opus \
            --enable-demuxer=aiff \
            --enable-demuxer=amr            
            \
            --enable-decoder=mp3 \
            --enable-decoder=aac \
            --enable-decoder=flac \
            --enable-decoder=pcm_s16le \
            --enable-decoder=pcm_s24le \
            --enable-decoder=pcm_s16be \
            --enable-decoder=pcm_s24be \  
            --enable-decoder=amrnb
            --enable-decoder=amrwb                 
            --enable-decoder=alac \
            --enable-decoder=vorbis \
            --enable-decoder=opus \
            --enable-decoder=ape \
            --enable-decoder=wmav2 \
            \
            --enable-parser=mpegaudio \
            --enable-parser=aac \
            --enable-parser=vorbis \
            --enable-parser=opus \
            --enable-parser=flac \
            \
            --enable-encoder=libmp3lame \
            \
            --enable-muxer=mp3

      - name: Build FFmpeg
        run: |
          cd ffmpeg-8.1.1

          make -j"$(nproc)"
          make install

      - name: Prepare dist
        run: mkdir -p dist

      - name: Copy binary
        run: |
          cp ffmpeg-8.1.1/build/bin/ffmpeg dist/audio-linux

      - name: Strip binary
        run: |
          strip dist/audio-linux

      - name: Ensure executable
        run: chmod +x dist/audio-linux

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: audio-linux
          path: dist/audio-linux

  build-ffmpeg-macos:
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install build dependencies
        run: |
          brew install \
            yasm \
            nasm \
            automake \
            autoconf \
            libtool

      - name: Download LAME
        run: |
          curl -L \
            https://downloads.sourceforge.net/project/lame/lame/3.100/lame-3.100.tar.gz \
            -o lame.tar.gz

          tar xf lame.tar.gz

      - name: Build LAME
        run: |
          cd lame-3.100

          ./configure \
            --disable-shared \
            --enable-static \
            --prefix="$PWD/../lame-build"

          make -j"$(sysctl -n hw.ncpu)"
          make install

      - name: Download FFmpeg
        run: |
          curl -L \
            https://ffmpeg.org/releases/ffmpeg-8.1.1.tar.xz \
            -o ffmpeg.tar.xz

          tar xf ffmpeg.tar.xz

      - name: Configure FFmpeg
        run: |
          cd ffmpeg-8.1.1

          export PKG_CONFIG_PATH="$PWD/../lame-build/lib/pkgconfig"

          ./configure \
            --prefix="$PWD/build" \
            --pkg-config-flags="--static" \
            --extra-cflags="-I$PWD/../lame-build/include" \
            --extra-ldflags="-L$PWD/../lame-build/lib" \
            \
            --disable-debug \
            --disable-doc \
            --disable-ffplay \
            --disable-ffprobe \
            --disable-network \
            --disable-everything \
            \
            --enable-ffmpeg \
            \
            --enable-protocol=file \
            --enable-protocol=pipe \
            \
            --enable-libmp3lame \
            \
            --enable-demuxer=mp3 \
            --enable-demuxer=wav \
            --enable-demuxer=ogg \
            --enable-demuxer=flac \
            --enable-demuxer=mov \
            --enable-demuxer=matroska \
            --enable-demuxer=aac \
            --enable-demuxer=ape \
            --enable-demuxer=asf \
            --enable-demuxer=opus \
            --enable-demuxer=aiff \   
            --enable-demuxer=amr            
            \
            --enable-decoder=mp3 \
            --enable-decoder=aac \
            --enable-decoder=flac \
            --enable-decoder=pcm_s16le \
            --enable-decoder=pcm_s24le \
            --enable-decoder=pcm_s16be \
            --enable-decoder=pcm_s24be \      
            --enable-decoder=amrnb
            --enable-decoder=amrwb                 
            --enable-decoder=alac \
            --enable-decoder=vorbis \
            --enable-decoder=opus \
            --enable-decoder=ape \
            --enable-decoder=wmav2 \
            \
            --enable-parser=mpegaudio \
            --enable-parser=aac \
            --enable-parser=vorbis \
            --enable-parser=opus \
            --enable-parser=flac \
            \
            --enable-encoder=libmp3lame \
            \
            --enable-muxer=mp3

      - name: Build FFmpeg
        run: |
          cd ffmpeg-8.1.1

          make -j"$(sysctl -n hw.ncpu)"
          make install

      - name: Prepare dist
        run: mkdir -p dist

      - name: Copy binary
        run: |
          cp ffmpeg-8.1.1/build/bin/ffmpeg dist/audio-macos

      - name: Strip binary
        run: |
          strip -x dist/audio-macos

      - name: Ensure executable
        run: chmod +x dist/audio-macos

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: audio-macos
          path: dist/audio-macos
          
  build-image-linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout    
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: x86_64-unknown-linux-musl

      - name: Install musl tools
        run: sudo apt-get update && sudo apt-get install -y musl-tools && sudo apt-get install -y nasm

      - name: Build release
        run: |
          cargo build --release \
            --manifest-path packages/image/Cargo.toml \
            --target x86_64-unknown-linux-musl

      - name: Prepare dist
        run: mkdir -p dist

      - name: Prepare artifact
        run: cp "packages/image/target/x86_64-unknown-linux-musl/release/image" dist/image-linux

      - name: Strip binary
        run: |
          strip -x dist/image-linux
          
      - name: Ensure executable
        run: chmod +x dist/image-linux

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: image-linux
          path: dist/image-linux

  build-image-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout    
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1

      - name: Build release
        run: |
          cargo build --release \
            --manifest-path packages/image/Cargo.toml

      - name: Prepare dist
        run: mkdir -p dist

      - name: Prepare artifact
        run: cp "packages/image/target/release/image" dist/image-macos

      - name: Strip binary
        run: |
          strip -x dist/image-macos
          
      - name: Ensure executable
        run: chmod +x dist/image-macos

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: image-macos
          path: dist/image-macos

  build-markitdown-linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1

      - name: Install markitdown CLI
        run: cargo install markitdown

      - name: Prepare dist
        run: mkdir -p dist

      - name: Copy binary
        run: |
          cp "$HOME/.cargo/bin/markitdown" dist/document-linux

      - name: Strip binary
        run: |
          strip -x dist/document-linux
          
      - name: Ensure executable
        run: chmod +x dist/document-linux

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: document-linux
          path: dist/document-linux

  build-markitdown-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1

      - name: Install markitdown CLI
        run: cargo install markitdown

      - name: Prepare dist
        run: mkdir -p dist

      - name: Copy binary
        run: |
          cp "$HOME/.cargo/bin/markitdown" dist/document-macos

      - name: Strip binary
        run: |
          strip -x dist/document-macos
          
      - name: Ensure executable
        run: chmod +x dist/document-macos
        
      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: document-macos
          path: dist/document-macos

  commit:
    needs: [
      build-ffmpeg-linux,
      build-ffmpeg-macos,    
      build-image-linux,
      build-image-macos,
      build-markitdown-linux, 
      build-markitdown-macos
    ]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - name: Ensure executables
        run: chmod +x dist/*
        
      - name: Commit binaries
        uses: EndBug/add-and-commit@v10
        with:
          add: "dist"
          message: "chore: update binaries"
